node_modules
dist
dist.zip
.DS_Store

.vercel
.env*

# iOS build artifacts (regenerated by `npx cap sync ios` / Xcode /
# CocoaPods) — never the source of truth, always excluded so a
# fresh clone + `pod install` regenerates them clean on any machine.
ios/App/App/public
ios/App/Pods
ios/App/Podfile.lock
ios/App/build
ios/App/DerivedData
ios/**/*.xcuserstate
ios/**/xcuserdata/
preview.log
suralink-full-codebase.txt
